[project]
name = "youtube-downloading"
version = "0.1.0"
description = "YouTube Playlist Downloader with CustomTkinter GUI"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "yt-dlp>=2025.3",
    "customtkinter>=5.2",
    "Pillow>=10.0",
]

[tool.uv]
package = false
